services:
  - type: web
    name: easyway-backend
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: AUTH_SECRET
        generateValue: true
